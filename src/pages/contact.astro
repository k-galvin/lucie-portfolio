---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contact | Lucie Galvin">
  <div class="contact-container">
    <div class="contact-card fade-in">
      <div class="contact-header">
        <h2 class="contact-title">Get in Touch</h2>
        <a href="mailto:studio@luciegalvin.com" class="contact-email-link">studio@luciegalvin.com</a>
        <p class="contact-subtitle">
          For inquiries about commissions, representation, or purchasing original works.
        </p>
      </div>

      <!-- Contact Form -->
      <form id="contact-form" class="contact-form">
        <input type="text" name="_honey" style="display:none" />
        <div class="form-group">
          <label for="contact-name" class="form-label">Name</label>
          <input type="text" id="contact-name" name="name" class="form-input" required />
        </div>

        <div class="form-group">
          <label for="contact-email" class="form-label">Email</label>
          <input type="email" id="contact-email" name="email" class="form-input" required />
        </div>

        <div class="form-group">
          <label for="contact-message" class="form-label">Message</label>
          <textarea id="contact-message" name="message" rows="4" class="form-textarea" required></textarea>
        </div>

        <button type="submit" id="submit-btn" class="submit-btn">
          <span class="submit-text-normal">Send Message</span>
          <span class="submit-text-loading hidden">Sending...</span>
        </button>

        <!-- Message Status Triggers -->
        <div id="status-success" class="status-message success hidden">
          Your message has been sent successfully!
        </div>

        <div id="status-error" class="status-message error hidden">
          An error occurred. Please try again later.
        </div>
      </form>
    </div>
  </div>
</Layout>

<style>
  .contact-container {
    width: 100%;
    height: 100%;
    max-width: 1600px;
    margin: 0 auto;
    padding: 2rem;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .contact-card {
    width: 100%;
    max-width: 580px;
    background-color: var(--color-bg);
    border: 1px solid var(--color-border);
    padding: 2.5rem 3rem;
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.01);
  }

  .contact-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .contact-title {
    font-size: 1.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text);
    margin-bottom: 0.5rem;
  }

  .contact-email-link {
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-hockney-blue);
    text-decoration: none;
    transition: color 0.2s ease;
    display: block;
    margin-bottom: 0.75rem;
  }

  .contact-email-link:hover {
    color: var(--color-hockney-pink);
    text-decoration: underline;
  }

  .contact-subtitle {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .form-label {
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-text-muted);
  }

  .form-input, .form-textarea {
    width: 100%;
    background-color: transparent;
    border: none;
    border-bottom: 1.5px solid var(--color-border);
    padding: 0.5rem 0;
    font-family: var(--font-sans);
    font-size: 0.95rem;
    color: var(--color-text);
    border-radius: 0;
    transition: border-color 0.3s ease;
  }

  .form-input:focus, .form-textarea:focus {
    outline: none;
    border-bottom-color: var(--color-hockney-blue); /* Focus bottom border matches Hockney pool blue */
  }

  /* Custom input colors on hover */
  .form-input:hover:not(:focus), .form-textarea:hover:not(:focus) {
    border-bottom-color: var(--color-hockney-pink);
  }

  .form-textarea {
    resize: none;
  }

  .submit-btn {
    width: 100%;
    padding: 0.85rem;
    background-color: var(--color-text);
    color: var(--color-bg);
    font-weight: 600;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    transition: background-color 0.3s ease, transform 0.2s ease;
    margin-top: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .submit-btn:hover {
    background-color: var(--color-hockney-pink); /* Hockney pink hover */
    transform: translateY(-1px);
  }

  .submit-btn:active {
    transform: translateY(0);
  }

  .submit-btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
    transform: none;
  }

  .hidden {
    display: none !important;
  }

  /* Status messages styling */
  .status-message {
    padding: 0.75rem 1rem;
    font-size: 0.85rem;
    font-weight: 500;
    text-align: center;
    margin-top: 0.5rem;
    animation: fadeIn 0.3s ease forwards;
  }

  .status-message.success {
    background-color: rgba(57, 181, 74, 0.08);
    color: var(--color-hockney-green);
    border: 1px solid var(--color-hockney-green);
  }

  .status-message.error {
    background-color: rgba(255, 91, 132, 0.08);
    color: var(--color-hockney-pink);
    border: 1px solid var(--color-hockney-pink);
  }

  @media (max-width: 768px) {
    .contact-container {
      padding: 1.5rem 1rem;
      align-items: flex-start;
      overflow-y: auto;
    }
    .contact-card {
      padding: 1.5rem;
      border: none;
      box-shadow: none;
      margin-top: 1rem;
      margin-bottom: 1rem;
      max-width: 100%;
    }
    .contact-title {
      font-size: 1.4rem;
    }
  }
</style>

<script>
  import { db } from '../lib/firebase';
  import { collection, addDoc, serverTimestamp } from 'firebase/firestore';

  function initContactForm() {
    const form = document.getElementById('contact-form') as HTMLFormElement;
    if (!form) return;

    const btnSubmit = document.getElementById('submit-btn') as HTMLButtonElement;
    const txtNormal = btnSubmit?.querySelector('.submit-text-normal');
    const txtLoading = btnSubmit?.querySelector('.submit-text-loading');
    const statusSuccess = document.getElementById('status-success');
    const statusError = document.getElementById('status-error');

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      // Clear statuses
      statusSuccess?.classList.add('hidden');
      statusError?.classList.add('hidden');

      // Loading State
      if (btnSubmit) btnSubmit.disabled = true;
      txtNormal?.classList.add('hidden');
      txtLoading?.classList.remove('hidden');

      try {
        const nameInput = document.getElementById('contact-name') as HTMLInputElement;
        const emailInput = document.getElementById('contact-email') as HTMLInputElement;
        const messageInput = document.getElementById('contact-message') as HTMLTextAreaElement;
        const honeyInput = form.querySelector('input[name="_honey"]') as HTMLInputElement;

        const nameVal = nameInput?.value || '';
        const emailVal = emailInput?.value || '';
        const messageVal = messageInput?.value || '';
        const honeyVal = honeyInput?.value || '';

        // 1. Submit backup to Firestore database
        await addDoc(collection(db, 'messages'), {
          name: nameVal,
          email: emailVal,
          message: messageVal,
          submittedAt: serverTimestamp()
        });

        // 2. Submit to FormSubmit.co for email forwarding
        const response = await fetch("https://formsubmit.co/ajax/studio@luciegalvin.com", {
          method: "POST",
          headers: { 
            'Content-Type': 'application/json',
            'Accept': 'application/json'
          },
          body: JSON.stringify({
            name: nameVal,
            email: emailVal,
            message: messageVal,
            _honey: honeyVal,
            _captcha: "false",
            _subject: `New contact from ${nameVal}`
          })
        });

        if (response.ok) {
          statusSuccess?.classList.remove('hidden');
          form.reset();
        } else {
          const errorData = await response.json().catch(() => ({}));
          console.error("FormSubmit Error:", errorData);
          statusError?.classList.remove('hidden');
        }
      } catch (err) {
        console.error("Contact Submission Error:", err);
        statusError?.classList.remove('hidden');
      } finally {
        // Reset states
        if (btnSubmit) btnSubmit.disabled = false;
        txtNormal?.classList.remove('hidden');
        txtLoading?.classList.add('hidden');
      }
    });
  }

  // Bind to page transitions
  document.addEventListener('astro:page-load', initContactForm);
</script>
